# 前端样式说明

这份文档给后续接手前端 UI 的人使用。当前前端已经重构为“简约工作台”风格：扁平、克制、留白明确，适合聊天、知识库和后台管理这种需要长期盯屏操作的产品界面。

## 设计定位

项目不是营销页，也不是炫技型 AI 官网。第一屏必须是可用的登录、聊天或后台操作界面。

- 整体气质：轻量、安静、工程感、文档式。
- 页面结构：左侧导航 / 中央工作区 / 底部输入或表格操作。
- 视觉重点：信息层级、输入效率、表格扫描、弹窗确认。
- 禁止回退到蜂窝发光背景、玻璃拟态、大面积渐变、厚重阴影、紫蓝赛博风。
- 常规容器圆角以 `14px` 为基准，composer、弹窗、悬浮索引卡可以放到 `24px` 到 `28px`；不要回到尖锐方框。

## UI 修改流程

以后每次修改 UI，都必须同时使用这两个技能检查：

```text
$gpt-taste
$minimalist-ui
```

规则：

- 先用 `$gpt-taste` 做交互、按钮对比度、布局密度和动效检查。
- 再用 `$minimalist-ui` 做扁平、白底、浅边框、少阴影和灰色层级检查。
- 如果只是修一个微交互，也要确认没有被通用按钮规则误伤。
- 修改后把新形成的可复用规则补进这份文档。

## 字体

用户要求的字体应写作 `Cascadia Mono`，不是 `casdia mono`。

全局字体保持：

```css
font-family: "Cascadia Mono", "Microsoft YaHei", Consolas, monospace;
```

规则：

- 不引入 Inter、Roboto、Open Sans。
- 不使用 viewport width 控制字体大小。
- `letter-spacing` 保持 `0`，不做负字距。
- 中文正文优先可读性；数字、ID、模型名保留 mono 的工程感。
- 页面标题不宜过大，后台和聊天界面不是 hero 站点。

## 色彩系统

主色固定为 `#4C4F69`。后续不要新增模块主色。

```text
主色: #4C4F69
主色 hover: #3f4157
正文: #202123
弱文本: rgba(48, 52, 70, 0.58)
页面底色: #F7F6F3
工作区底色: #FBFBFA
卡片 / 面板: #FFFFFF
弱面板: #F9F9F8
边框: #EAEAEA
危险色: #7f1d1d
危险底: #FDEBEC
成功色: #346538
成功底: #EDF3EC
警告色: #956400
警告底: #FBF3DB
信息色: #1F6C9F
信息底: #E1F3FE
```

规则：

- 大面积区域只用浅暖灰和白色。
- 主色只用于主按钮、发送箭头、当前导航、Think 开启态和关键选中态。
- 状态色只用于状态胶囊、错误提示和确认弹窗，不扩散成模块主题。
- 背景不使用彩色渐变；需要层次时用边框、留白和极轻阴影。

## 布局

### 登录页

- 登录卡片居中，白底、`1px` 边框、`14px` 到 `24px` 圆角。
- 不展示营销 hero，不增加说明性大段文案。
- 登录 / 注册切换是分段控制，激活态使用主色。
- 打开登录界面时登录卡片使用轻量 reveal：`opacity + translateY(14px) + scale(0.985)` 到稳定状态，约 `0.52s`；尊重 `prefers-reduced-motion`。

### 会话页

- `.app-shell` 是主结构，左侧导航展开宽度约 `292px`，折叠后 `64px`。
- 聊天工作区使用 `#FBFBFA`，顶部栏和底部 composer 使用白底。
- 顶部会话信息栏是横向直角长方形条，不做卡片边缘：无圆角、无边框、无阴影、无 blur。
- 新会话欢迎语保留，但不能做成营销页标题。
- 用户消息使用 `#4C4F69` 主色气泡包裹，白字、右对齐；助手消息不需要气泡，直接平铺在背景上。
- 用户和助手消息之间不加横向分割线，靠留白区分上下文。
- 消息行宽度必须和 composer 一致，统一使用 `--chat-content-width`，不能出现消息比输入框窄一截或宽一截。
- 中窄窗口下聊天页必须优先保证消息可读：当宽度不足时隐藏侧栏/右侧定位 rail，让 `.chat` 占满视口；不能让消息列被压成逐字竖排。
- Composer 保持底部输入效率，整体是圆润悬浮输入条；按钮和模型选择都必须清晰可点。
- 发送按钮正常状态使用圆形上箭头图标，发送中保持同尺寸圆形按钮并显示清晰的停止方块图标；停止方块不能太小，不要把按钮拉宽成“中断”文字按钮。
- Composer 下方固定显示小字：`AI 也可能会犯错。请核查重要信息。`
- Composer 是悬浮控制条，不占用聊天滚动布局行；不要在底部额外铺一块白色幕布。
- 消息列表必须预留 composer 的底部安全区，避免最后一条消息被悬浮输入框遮住或截断。
- 会话进度 rail 按视口高度延伸，从顶部内容区到 composer 上方，不使用固定 `300px` 的短轨道。
- 会话进度 rail 靠右显示为可轻微倾斜的用户消息刻度，不显示贯穿整栏的竖线；用户对话几次就显示几条，不做窗口裁剪。刻度要紧凑，横线比 ChatGPT 参考图略宽但不能松散。
- 切换会话时右侧会话进度 rail 必须立即清空旧会话刻度，不允许旧 tick 离场动画和新 tick 同时存在。
- 会话进度弹窗只展示用户消息，不展示 AI 输出，也不显示 token。
- 会话进度弹窗必须适配项目滚动条样式；只允许纵向滚动，禁止横向滚动条和原生上下左右箭头。滚动条要嵌在弹窗右侧内边距里，不能越过圆角边界。
- 弹窗与消息刻度之间要有外层 hover 重叠区和短延迟关闭，避免鼠标从刻度移动到弹窗时弹窗消失；不要在弹窗滚动容器本身放会制造横向溢出的外伸伪元素。
- Markdown 代码块使用浅灰卡片样式，复制按钮使用叠方块图标 + “复制”，不要使用主色深底，避免和用户气泡撞色。
- 用户主色气泡内如果出现强调、链接、行内代码等本来使用主题色的元素，必须改为白色或白色透明底，保证可读。
- 用户气泡内的真正突出显示 `mark` 必须使用白底黑字，不能沿用气泡底色、主题色或灰色高亮；内联代码可以保留半透明白底，但高亮要清楚反转出来。
- 用户气泡内的浏览器文本选区 `::selection` 也必须白底黑字；全局选区可以继续主题色底白字，但不能让跨消息拖选时用户气泡段落消失在同色背景里。
- 历史会话列表使用 ChatGPT 式简洁列表：只显示标题，不显示时间副文本；普通 hover 不变色，当前正在查看的会话使用浅灰背景。
- 历史会话标题本身不放图标；每条历史会话左侧放一个轻量聊天气泡图标，图标和标题稳定一行对齐。
- 左侧导航栏不能做成有边缘感的圆角卡片，主侧栏、搜索区和历史会话面板保持直角、无边框、无阴影。
- 搜索聊天控件本身只保留外层单边框浅色圆角输入条；内部 `input` 不能再显示独立边框或浏览器 focus ring，图标和 placeholder 必须留足间距，未聚焦空态不显示黑色光标，点击聚焦后要显示接近 AI 输入框高度的主题色闪烁竖线，`Ctrl K` 使用 Cascadia Mono 并跟随主题弱灰样式。
- 历史会话行不使用 hover 变色；只有当前会话使用浅灰 active 态，保留纯文本列表和必要的更多操作按钮。
- Composer 聚焦时只允许外层输入容器显示焦点反馈，内部 `textarea` 不再画单独 outline 或灰色方框。
- Think 未开启时必须是白底、浅边框、深灰文字；小字使用 `rgba(48, 52, 70, 0.58)`。
- Think 开启后才使用 `#4C4F69` 背景和白字。
- 模型下拉列表里只有当前选中项和 hover 项使用主色，其他模型项保持白底；模型 ID 小字不能压暗到不可读。

### 后台页

- `.admin-page` 使用左侧导航 + 主内容滚动区。
- 后台背景是 `#F7F6F3`，表格和指标卡是白色面板。
- Dashboard 指标使用扁平 bento 网格，不留空洞装饰格。
- Dashboard 消息曲线统一使用 ECharts 单折线图：默认展示今日 24 小时每小时消息数，可切换本月 30/31 天自然日消息数；主线色 `#4C4F69`，白心圆点，虚线网格，图表文字继承 Cascadia Mono。
- ECharts 空心圆点不能只设置 `itemStyle.color: #fff`，必须同时显式设置 `lineStyle.color: #4C4F69`，否则白底上会退化成散点图，曲线看起来没有连上。
- 消息曲线入场动效不能使用 ECharts 默认纵向拉伸，也不能边补数据边重算曲线；需要先用完整数据计算出最终平滑形状，再用 SVG `stroke-dashoffset` 从左到右揭示，点经过时从 `r=0` 放大到正常大小。
- 消息曲线平滑算法不能使用会过冲的 Catmull-Rom 控制点；控制点只在 X 方向拉开、Y 值固定在相邻端点，保证消息数曲线不会视觉低于 0 轴。
- 消息曲线的线条必须明显快于原点，但线本身也要缓慢铺开，避免开头冲刺；原点不能用独立慢时钟，而是跟着同一条时序窗出现，只在横轴位置到达后再展开，不能线点完全脱节。
- 消息曲线的点半径必须使用统一的最终半径和独立的 `easeOut` 成长曲线，默认可以收在约 `3.5px` 左右，不能因为索引越靠后就把点的视觉半径压小；否则后面的点会看起来越来越瘦。
- 消息曲线卡放在 Dashboard 指标卡下方，结构为左上“数据曲线 + ?”、右上范围切换、图表上方消息数统计胶囊；卡片白底、`1px #EAEAEA` 边框、14px 圆角、无厚阴影。
- 消息曲线右上范围切换使用登录页同款滑块分段控件，标签固定为 `24小时` / `本月`，active 文字为白色，滑块使用主题色。
- 后台图表只展示真实后端聚合数据，缺失小时或日期由后端补 0，前端不伪造趋势；tooltip 使用白底浅边框，小字号展示当前时段消息数即可。
- 表格以可扫描为第一优先级，避免不必要横向滚动。
- 页面内只保留一个主标题区，不重复堆标题。

## 组件规则

### 按钮

- 主按钮：`#4C4F69` 背景，白字。
- 次按钮：白底、浅边框、深灰蓝文字。
- hover 可以轻微上移 `translateY(-1px)`，但不加厚重阴影。
- active 可以轻微缩放或压下。
- 禁用态：浅灰底、弱文字、无阴影、`cursor: not-allowed`。
- 删除按钮默认红字白底，hover 变红底白字。

### 卡片和面板

```css
border: 1px solid #EAEAEA;
border-radius: 14px;
background: #FFFFFF;
box-shadow: 0 1px 2px rgba(76, 79, 105, 0.04);
```

规则：

- 不把卡片套进卡片。
- 不使用玻璃、发光、厚阴影。
- 面板内容靠留白和边框区分层级。

### 表格

- 表头背景：`#F9F9F8`。
- 行 hover：`#F8F7F4`。
- 操作列居中，按钮高度稳定。
- 后台表格工具栏里的状态筛选统一使用 `.kc-select-menu` 按钮式下拉，不能混用浏览器原生 `<select>`；失败任务、文档管理和分块管理的“全部状态”控件必须视觉一致。
- 后台列表卡片必须跟随主内容区缩小，宽内容优先让文本列压缩和省略；知识库列表操作按钮必须保持单行，不能用按钮换行来适配正常窗口。
- 文档管理列表第一列图标必须使用固定图标格和固定 SVG 尺寸，文件名长短不能影响图标大小。
- 文本可省略，但只有实际溢出时才显示 tooltip。
- 状态胶囊使用语义色，不改变整行背景。

### 弹窗

- 遮罩允许轻微 blur。
- 弹窗白底、浅边框、`24px` 左右圆角。
- 错误显示在弹窗内部。
- 底部操作按钮右对齐。
- 删除确认必须使用项目自定义弹窗，不使用浏览器原生 `confirm`。

### Tooltip

- 折叠导航 tooltip 和表格溢出 tooltip 统一为深色底白字。
- 只在必要场景显示，不要给普通按钮滥加 tooltip。
- tooltip 层级要高，不能被表格或弹窗裁剪。

## 动效

项目现在引入 `gsap`，统一通过 `frontend/src/utils/quietMotion.js` 使用。

规则：

- 动效只做进入 reveal：`opacity + translateY(12px)`，节奏约 `0.52s`。
- 后台滚动区使用 `ScrollTrigger`，但不做 pinned section、横向大片滚动。
- 动效服务于“进入时有秩序”，不能影响表格扫描和输入效率。
- hover / menu / modal 过渡保持 `160ms` 到 `240ms`。
- 不使用会导致布局抖动的动画属性，如 `top`、`left`、`width`、`height`。

## 图标

当前项目继续使用内联 SVG，统一规则：

```css
stroke: currentColor;
stroke-width: 1.9;
stroke-linecap: round;
stroke-linejoin: round;
fill: none;
```

规则：

- 导航项、标题、主要操作按钮优先使用图标 + 文案。
- 图标颜色跟随 `currentColor`。
- 不新增多套图标风格。
- 品牌旋涡 logo 必须使用单条连续路径表达；favicon、登录页和导航栏品牌图标都使用右上 45 度的内核焦点补偿，不要再使用中心点加多段断裂弧线的结构。
- 历史会话行的左侧图标必须放在固定宽度图标格中，SVG 使用 `display: block`，图标格、标题文字和整行按钮都按垂直中心线对齐，避免图标因内联基线看起来下沉。
- 折叠导航栏的品牌 logo 使用固定图标盒居中；旋涡方向补偿写在 SVG 内部，rail 层不要再叠加额外位移，避免图标偏离方框中心。

## 继续改 UI 的检查清单

- 是否保留了 `#4C4F69` 和 `Cascadia Mono`？
- 是否仍然是产品工作台，而不是营销页？
- 是否没有蜂窝发光、玻璃拟态、大面积渐变、厚阴影？
- 常规容器是否保持约 `14px` 圆角，composer / 弹窗 / 悬浮索引是否保持 `24px` 到 `28px`？
- 会话消息是否平铺在背景上，且消息行宽与 composer 一致？
- 发送按钮是否是圆形箭头，composer 下方是否保留 AI 风险提示小字？
- 右侧定位 rail 是否从内容区延伸到 composer 上方，hover 是否能看到具体消息弹窗？
- 主按钮是否主色底白字，hover 后仍可读？
- 表格是否可扫描，操作列是否稳定？
- 错误是否显示在当前上下文或弹窗内部？
- 删除是否使用自定义确认弹窗？
- tooltip 是否只在折叠导航或真实溢出时出现？
- 动效是否只做轻量 reveal，没有干扰输入和管理操作？
